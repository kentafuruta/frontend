<%
var absolutePath = __filename.split(site.develop)[__filename.split(site.develop).length -1].replace('.ejs','.html');
var relativePath = '../'.repeat([absolutePath.split('/').length -1]);
var page = {
  layout: "_default",
  title: "トップページ",
  description: site.description,
  keywords: "",
  bodyClass: "",
  singleCss: [],
  singleJs: [],
  ogpType: "website",
  ogpImage: site.ogpImage,
  absolutePath: absolutePath,
  relativePath: relativePath
};
_%>
<% content = `
      <h1>トップページ!</h1>
      <p>本文</p>
` _%>
<%- include(page.relativePath + '_layout/' + page.layout, {site: site, page: page}); _%>